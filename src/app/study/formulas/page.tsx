"use client";import{useState}from"react";import{motion}from"framer-motion";import{Search,Calculator}from"lucide-react";import{formulas}from"@/lib/data";export default function FormulaBankPage(){const[search,setSearch]=useState("");const[subj,setSubj]=useState("all");const filtered=formulas.filter(s=>subj==="all"||s.subject.toLowerCase().includes(subj)).flatMap(s=>s.items.map(f=>({...f,subject:s.subject}))).filter(f=>f.name.toLowerCase().includes(search.toLowerCase())||f.f.toLowerCase().includes(search.toLowerCase()));return(<div className="max-w-4xl mx-auto px-4 py-8"><div className="mb-8"><h1 className="text-3xl font-bold mb-2 flex items-center gap-2"><Calculator className="w-8 h-8 text-brand-700"/>Formula Bank</h1></div><div className="flex gap-3 mb-8"><div className="relative flex-1"><Search className="absolute left-4 top-1/2 -translate-y-1/2 w-4 h-4 text-[var(--fg3)]"/><input type="text"placeholder="Search formulas..."value={search}onChange={e=>setSearch(e.target.value)}className="input pl-11"/></div><select value={subj}onChange={e=>setSubj(e.target.value)}className="input w-auto"><option value="all">All</option><option value="mathematics">Math</option><option value="physics">Physics</option><option value="chemistry">Chemistry</option></select></div><div className="space-y-4">{filtered.map((f,i)=>(<motion.div key={`${f.name}-${i}`}initial={{opacity:0,y:10}}animate={{opacity:1,y:0}}transition={{delay:i*0.05}}className="card card-p"><div className="flex items-center justify-between mb-2"><span className="text-xs font-medium text-[var(--fg3)] uppercase">{f.subject}</span><span className="badge badge-b">{f.name}</span></div><div className="p-4 bg-gradient-to-r from-brand-50 to-purple-50 dark:from-brand-900/10 dark:to-purple-900/10 rounded-xl text-center"><code className="text-lg font-bold text-brand-900 dark:text-brand-300">{f.f}</code></div></motion.div>))}</div></div>)}