"use client";import{useState}from"react";import{motion}from"framer-motion";import{Search,Volume2}from"lucide-react";import{dictionary}from"@/lib/data";export default function DictionaryPage(){const[search,setSearch]=useState("");const filtered=dictionary.filter(d=>d.term.toLowerCase().includes(search.toLowerCase())||d.def.toLowerCase().includes(search.toLowerCase()));const speak=(w:string)=>{if("speechSynthesis"in window){const u=new SpeechSynthesisUtterance(w);u.rate=0.8;window.speechSynthesis.speak(u)}};return(<div className="max-w-4xl mx-auto px-4 py-8"><div className="mb-8"><h1 className="text-3xl font-bold mb-2">📖 Educational Dictionary</h1></div><div className="relative mb-8"><Search className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-[var(--fg3)]"/><input type="text"placeholder="Search for a term..."value={search}onChange={e=>setSearch(e.target.value)}className="input pl-12 text-lg"/></div><div className="space-y-4">{filtered.map((d,i)=>(<motion.div key={d.term}initial={{opacity:0,y:10}}animate={{opacity:1,y:0}}transition={{delay:i*0.04}}className="card card-p"><div className="flex items-center gap-2 mb-2"><h3 className="text-xl font-bold">{d.term}</h3><button onClick={()=>speak(d.term)}className="p-1.5 rounded-lg hover:bg-[var(--bg3)]"><Volume2 className="w-4 h-4 text-brand-700"/></button></div><p className="text-sm text-[var(--fg3)] italic mb-2">{d.pron}</p><p className="text-[var(--fg2)] mb-3">{d.def}</p><div className="p-3 bg-[var(--bg2)] rounded-xl"><p className="text-sm"><span className="font-medium">Example:</span> {d.example}</p></div></motion.div>))}</div></div>)}