"use client";import Link from"next/link";import{usePathname}from"next/navigation";import{motion}from"framer-motion";import{Home,BookOpen,Brain,FileQuestion,User}from"lucide-react";const items=[{icon:Home,label:"Home",href:"/"},{icon:BookOpen,label:"Subjects",href:"/subjects"},{icon:Brain,label:"Practice",href:"/cbt/practice"},{icon:FileQuestion,label:"CBT",href:"/cbt"},{icon:User,label:"Profile",href:"/profile"}];export default function MobileNav(){const pathname=usePathname();const isActive=(href:string)=>href==="/"?pathname==="/":pathname.startsWith(href);return(<nav className="lg:hidden fixed bottom-0 left-0 right-0 z-50 bg-[var(--bg)]/95 backdrop-blur-xl border-t border-[var(--border)]"><div className="flex items-center justify-around h-16 max-w-lg mx-auto">{items.map(item=>{const active=isActive(item.href);return(<Link key={item.href}href={item.href}className={`flex flex-col items-center justify-center gap-0.5 px-3 py-1 rounded-xl transition-all duration-200 relative min-w-[60px] ${active?"text-brand-700 dark:text-brand-400":"text-[var(--fg3)]"}`}>{active&&<motion.div layoutId="mobile-tab"className="absolute inset-0 bg-brand-50 dark:bg-brand-900/20 rounded-xl"transition={{type:"spring",stiffness:500,damping:35}}/>}<item.icon className="w-5 h-5 relative z-10"/><span className="text-[10px] font-medium relative z-10">{item.label}</span></Link>)})}</div></nav>);}